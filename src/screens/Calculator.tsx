import React from 'react'
import { View, Text } from 'react-native';
import { AppTheme } from '../theme';

export const CalculatorScreen = () => {
    return (
        <View>
            <Text style={ AppTheme.text }>
                Calculator Screen
            </Text>     
        </View>
    )
}
